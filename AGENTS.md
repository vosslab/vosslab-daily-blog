# Project instructions

- Read `README.md`, `docs/operations.md`, and `docs/CODE_ARCHITECTURE.md` before publisher changes.
- Follow `docs/REPO_STYLE.md`, `docs/PYTHON_STYLE.md`, `docs/MARKDOWN_STYLE.md`, and
  `docs/PYTEST_STYLE.md`.
- Record code and contract changes in `docs/CHANGELOG.md`.
- This repository imports complete publication bundles. Generation, Git evidence, model execution,
  and scheduling belong to `vosslab-podcast`.
- Use Python 3.12; `source source_me.sh` establishes repository imports.
- Run focused pytest coverage for changed code and a strict staged MkDocs build for publication
  changes.
