# Project instructions

- For publisher work, read `README.md`, `docs/operations.md`, and `docs/CODE_ARCHITECTURE.md`.
- Follow `docs/REPO_STYLE.md`, `docs/PYTHON_STYLE.md`, `docs/MARKDOWN_STYLE.md`, and `docs/PYTEST_STYLE.md`; record code and contract changes in `docs/CHANGELOG.md`.
- Use `docs/INSTALL.md`, `docs/USAGE.md`, and `docs/FILE_STRUCTURE.md` for environment, commands, and file-routing details.
- This repository imports complete publication bundles. Generation, Git evidence, model execution, and scheduling belong to `vosslab-podcast`.
- Use Python 3.13; `source source_me.sh` establishes repository imports.
- Run focused pytest coverage for changed code and a strict staged MkDocs build for publication changes.
